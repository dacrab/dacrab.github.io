---
// Footer component extracted from Contact.astro
---

<footer class="footer-min">
  <div class="swiss-container">
    <div class="row">
      <span>© 2025 Vaggelis (DaCrab) — made with <span class="heart">❤️</span></span>
      <div class="links">
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#contact">Contact</a>
      </div>
    </div>
  </div>
</footer>

<style>
  .footer-min { background: #050505; color: #cfd6e3; border-top: 1px solid rgba(255,255,255,0.06); padding: 10px 0; }
  .row { display: flex; align-items: center; justify-content: space-between; gap: var(--space-4); min-height: 40px; }
  .links { display: inline-flex; gap: var(--space-3); }
  .links a { color: #aeb6c5; text-decoration: none; font-size: var(--font-size-xs); text-transform: uppercase; letter-spacing: 0.06em; font-weight: 600; }
  .links a:hover { color: white; }
  .heart { display: inline-block; }

  @media (max-width: 640px) {
    .row { flex-direction: column; align-items: flex-start; gap: var(--space-2); }
  }
</style>
