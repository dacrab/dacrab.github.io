<section id="about" class="swiss-section scroll-offset">
  <div class="section-number" aria-hidden="true">02</div>
  <div class="abstract-shapes" aria-hidden="true">
    <div class="shape-circle"></div>
    <div class="shape-bar"></div>
  </div>
  <div class="swiss-container">
    <header class="head section-header-spacing">
      <span class="section-eyebrow">About</span>
      <h2 class="title swiss-heading-2">Practical UI engineering</h2>
      <p class="desc">
        I build fast, accessible interfaces with clear structure and
        maintainable code.
      </p>
    </header>

    <div class="about-grid">
      <div class="about-main">
        <h3 class="swiss-heading-4">Profile</h3>
        <p class="swiss-body">
          Web developer based in Greece. I focus on reliable <span class="khl"
            >UI implementation</span
          >, pragmatic <span class="khl">architecture</span>, and
          production‑grade <span class="khl">performance</span>.
        </p>

        <h3 class="swiss-heading-4">Focus</h3>
        <ul class="bullets">
          <li>
            <span class="khl">Frontend engineering</span> and <span class="khl"
              >design systems</span
            >
          </li>
          <li>
            <span class="khl">Performance</span>, <span class="khl"
              >accessibility</span
            >, and <span class="khl">testing</span>
          </li>
          <li>
            <span class="khl">Developer experience</span> and <span class="khl"
              >tooling</span
            >
          </li>
        </ul>
      </div>

      <aside class="about-aside">
        <dl class="facts">
          <div class="row">
            <dt class="label label-normal">Role</dt><dd>Web Developer</dd>
          </div>
          <div class="row">
            <dt class="label label-normal">Location</dt><dd>Greece (EET)</dd>
          </div>
          <div class="row">
            <dt class="label label-normal">Stack</dt><dd>
              TypeScript, React/Next.js, Astro, CSS
            </dd>
          </div>
          <div class="row">
            <dt class="label label-normal">Email</dt><dd>
              <a class="link" href="mailto:vkavouras@proton.me"
                >vkavouras@proton.me</a>
            </dd>
          </div>
          <div class="row">
            <dt class="label label-normal">Availability</dt><dd>
              Freelance / consulting
            </dd>
          </div>
        </dl>

        <div class="skills">
          <h4 class="swiss-caption">Capabilities</h4>
          <div class="tags">
            <span class="tag u-caps-xs">React</span>
            <span class="tag u-caps-xs">Next.js</span>
            <span class="tag u-caps-xs">Astro</span>
            <span class="tag u-caps-xs">TypeScript</span>
            <span class="tag u-caps-xs">CSS</span>
            <span class="tag u-caps-xs">Accessibility</span>
            <span class="tag u-caps-xs">Performance</span>
            <span class="tag u-caps-xs">Tooling</span>
          </div>
        </div>
      </aside>
    </div>
  </div>
</section>

<style>
  .abstract-shapes {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .shape-circle {
    position: absolute;
    width: 300px;
    height: 300px;
    border-radius: 50%;
    border: 2px solid var(--color-primary);
    opacity: 0.05;
    top: 20%;
    left: -150px;
  }

  .shape-bar {
    position: absolute;
    width: 4px;
    height: 400px;
    background: var(--color-primary);
    opacity: 0.03;
    top: 10%;
    right: 15%;
    transform: rotate(-8deg);
  }

  .head {
    position: relative;
    display: grid;
    gap: var(--space-6);
  }
  .head::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    width: 3px;
    height: 80px;
    background: var(--color-primary);
  }

  .section-eyebrow {
    padding-left: var(--space-6);
  }
  .title {
    padding-left: var(--space-6);
  }
  .desc {
    color: var(--color-text-secondary);
    max-width: 60ch;
    padding-left: var(--space-6);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-12);
    position: relative;
  }

  .about-grid::before {
    content: "";
    position: absolute;
    width: 2px;
    height: 100%;
    background: linear-gradient(
      to bottom,
      transparent,
      var(--color-border) 20%,
      var(--color-border) 80%,
      transparent
    );
    left: 50%;
    transform: translateX(-50%);
    opacity: 0.3;
  }
  .about-main {
    display: grid;
    gap: var(--space-6);
    align-content: start;
  }
  .about-aside {
    display: grid;
    gap: var(--space-6);
  }
  .bullets {
    list-style: none;
    padding: 0;
    display: grid;
    gap: var(--space-2);
    color: var(--color-text);
  }
  .bullets li {
    position: relative;
    padding-left: var(--space-5);
    line-height: 1.6;
  }
  .bullets li::before {
    content: "+";
    position: absolute;
    left: 0;
    top: 0;
    font-size: var(--font-size-lg);
    line-height: 1.6;
    font-weight: 700;
    color: var(--color-primary);
  }

  .facts {
    display: grid;
    gap: 0;
    background: var(--color-background);
    border: 2px solid var(--color-border);
    padding: 0;
  }
  .facts .row {
    display: grid;
    grid-template-columns: 120px 1fr;
    align-items: baseline;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    border-bottom: 1px solid var(--color-border);
  }
  .facts .row:last-child {
    border-bottom: 0;
  }

  dd {
    font-size: var(--font-size-sm);
  }

  .link {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 600;
    border-bottom: 2px solid var(--color-primary);
    transition:
      color var(--transition-fast),
      border-color var(--transition-fast);
    position: relative;
    display: inline-block;
  }
  .link:hover {
    color: var(--color-primary);
    border-bottom-width: 3px;
  }
  .link:active {
    transform: translateY(1px);
  }

  .skills {
    display: grid;
    gap: var(--space-3);
  }
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }
  .tag {
    border: 2px solid var(--color-border);
    background: var(--color-background);
    padding: var(--space-2) var(--space-3);
    font-weight: 700;
    transition:
      border-color var(--transition-fast),
      color var(--transition-fast),
      transform var(--transition-fast),
      box-shadow var(--transition-fast);
    cursor: default;
  }
  .tag:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
    transform: translateY(-1px);
    box-shadow: 2px 2px 0 var(--color-primary);
  }

  /* keyword highlight handled globally via .khl */

  @media (max-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }
    .facts .row {
      grid-template-columns: 100px 1fr;
      gap: var(--space-2);
      padding: var(--space-3);
    }
  }
</style>
