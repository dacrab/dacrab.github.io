<section id="about" class="bg-zinc-900 px-6 py-32 text-zinc-50 md:px-12">
  <div class="mx-auto max-w-4xl">
    <span class="about-label mb-8 block font-mono text-xs tracking-widest text-zinc-400 uppercase"
      >03 / Profile</span
    >

    <h2 class="about-heading mb-16 text-4xl leading-tight font-bold tracking-tight md:text-6xl">
      I build digital products with a focus on <span class="text-zinc-400">simplicity</span> and <span
        class="text-zinc-400">purpose</span
      >.
    </h2>

    <div
      class="grid grid-cols-1 gap-16 text-lg leading-relaxed text-zinc-400 md:grid-cols-2"
    >
      <div class="about-text">
        <p class="mb-6">
          I am a Junior Web Developer and recent graduate with a Diploma in Multimedia, Web & Video
          Game Development from IEK AKMI. I approach web development as an engineering discipline
          first, avoiding bloat and striving for zero-runtime overhead.
        </p>
        <p>
          My work focuses on building high-performance marketing sites and full-stack web apps that
          consistently achieve 95+ Lighthouse scores. I specialize in creating accessible,
          multilingual UIs and robust backend integrations.
        </p>
      </div>

      <div class="space-y-8">
        <div>
          <h3 class="mb-4 text-sm font-bold tracking-widest text-white uppercase">Stack</h3>
          <ul class="about-stack space-y-2 font-mono text-sm">
            <li>TypeScript / JavaScript</li>
            <li>Next.js / React / SvelteKit</li>
            <li>Astro / Tailwind CSS</li>
            <li>Supabase / PostgreSQL / Drizzle</li>
          </ul>
        </div>

        <div id="contact" class="about-contact">
          <h3 class="mb-4 text-sm font-bold tracking-widest text-white uppercase">Contact</h3>
          <a
            href="mailto:vkavouras@proton.me"
            class="inline-block text-2xl font-bold text-white transition-colors hover:text-zinc-500"
          >
            vkavouras@proton.me
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import gsap from 'gsap';

  const aboutTl = gsap.timeline({
    scrollTrigger: {
      trigger: '#about',
      start: 'top 75%',
      once: true,
    },
  });

  // Label fades in
  aboutTl.fromTo('.about-label',
    { opacity: 0, x: -20 },
    { opacity: 1, x: 0, duration: 0.6, ease: 'power2.out' }
  )
  // Heading scales up from slightly smaller
  .fromTo('.about-heading',
    { opacity: 0, scale: 0.95 },
    { opacity: 1, scale: 1, duration: 0.8, ease: 'power2.out' },
    '-=0.3'
  )
  // Text paragraph slides up
  .fromTo('.about-text',
    { opacity: 0, y: 30 },
    { opacity: 1, y: 0, duration: 0.8, ease: 'power2.out' },
    '-=0.4'
  )
  // Stack items stagger in
  .fromTo('.about-stack li',
    { opacity: 0, x: 20 },
    { opacity: 1, x: 0, duration: 0.4, stagger: 0.1, ease: 'power2.out' },
    '-=0.4'
  )
  // Contact fades in last
  .fromTo('.about-contact',
    { opacity: 0, y: 20 },
    { opacity: 1, y: 0, duration: 0.6, ease: 'power2.out' },
    '-=0.2'
  );
</script>
