{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "ultracite check . && tsc --noEmit && knip",
    "fix": "ultracite fix .",
    "ci": "bun run lint && bun run build"
  },
  "dependencies": {
    "astro": "5.14.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "knip": "^5.64.1",
    "typescript": "^5",
    "ultracite": "^5.5.0"
  }
}
