{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "ultracite check . && tsc --noEmit && knip",
    "fix": "ultracite fix .",
    "ci": "bun run lint && bunx astro check && bun run build"
  },
  "dependencies": {
    "astro": "5.14.6"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "knip": "^5.65.0",
    "typescript": "^5.9.3",
    "ultracite": "^5.6.3"
  }
}
