{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "ultracite check . && tsc --noEmit && knip",
    "fix": "ultracite fix .",
    "ci": "npm run lint && npx astro check && npm run build"
  },
  "dependencies": {
    "astro": "5.15.4"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "knip": "^5.67.1",
    "typescript": "^5.9.3",
    "@astrojs/check": "^0.9.5",
    "ultracite": "^6.3.2"
  }
}
