{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "lint": "ultracite lint .",
    "lint:fix": "ultracite format .",
    "format": "ultracite format .",
    "format:auto": "ultracite format .",
    "format:check": "ultracite lint .",
    "knip": "knip",
    "type-check": "tsc --noEmit",
    "ci": "bun run format:auto && bun run lint && bun run type-check && bun run build",
    "fix": "bun run lint:fix && bun run format",
    "check": "bun run lint && bun run type-check && bun run format:check"
  },
  "dependencies": {
    "@astrojs/react": "4.4.0",
    "astro": "5.14.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "knip": "^5.64.1",
    "typescript": "^5",
    "ultracite": "^4.2.13"
  }
}
